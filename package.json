{
  "name": "tts-service",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.20.1",
    "wrangler": "^3.0.0"
  }
}